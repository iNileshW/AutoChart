# Backend runtime configuration for AutoChart.
# Copy to .env for local development. Do NOT commit real secrets.

# uvicorn bindings (backend_runner reads these)
AUTOCHART_HOST=127.0.0.1
AUTOCHART_PORT=8000
AUTOCHART_RELOAD=0

# CORS: comma-separated allow-list. Wildcards not allowed with credentials.
AUTOCHART_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional API key. When set, every /api/* and /mcp request must send
# X-API-Key: <this-value>. Leave blank/unset to disable the gate.
AUTOCHART_API_KEY=
AUTOCHART_API_KEY_HEADER=X-API-Key

# Logging
AUTOCHART_LOG_LEVEL=INFO
AUTOCHART_LOG_JSON=1

# Observability
# Prometheus scrape endpoint at /metrics is on by default.
AUTOCHART_METRICS=1
# OpenTelemetry OTLP/HTTP endpoint (optional). If unset, tracing is disabled.
# Example: http://otel-collector:4318/v1/traces
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=autochart
# Sentry error reporting (optional).
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.0

# Grafana reverse proxy — expose a local Grafana under /grafana/*.
# Only used when the lab VM only forwards port 8000. Leave unset
# for direct :3000 access.
AUTOCHART_GRAFANA_UPSTREAM=
# When a stripping reverse proxy sits in front (e.g. /proxy/8000/),
# set this to the stripped prefix so Grafana's serve_from_sub_path
# expectations line up.
AUTOCHART_GRAFANA_UPSTREAM_PREFIX=
